{
  "name": "alarm-clock",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "typecheck": "npm run typecheck -w apps/api && npm run typecheck -w apps/web",
    "deploy": "npm run deploy -w apps/api && npm run deploy -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "overrides": {
    "framer-motion": "file:/Users/bikashtalukder/node_modules/framer-motion"
  }
}